# ==========================================================
#   Arquivo de parametros da simulacao closedloop
#   Uso: ./closedloop -c params_exemplo.txt
#
#   Formato: CHAVE=VALOR (tambem aceita "CHAVE VALOR")
#   Linhas em branco ou iniciadas com '#' sao ignoradas.
#   Prioridade: argumentos de linha de comando > este arquivo > default
#
#   Qualquer chave tambem pode ser sobrescrita direto na linha de
#   comando, por exemplo:
#     ./closedloop -c params_exemplo.txt --vf-startup 0 --rpm 800

# Referencia do motor
# D. Kumar, R. A. Gupta and N. Gupta, "Modeling and simulation of four switch three-phase BLDC motor using anti-windup PI controller," 
# 2017 Innovations in Power and Advanced Computing Technologies (i-PACT), 
# Vellore, India, 2017, pp. 1-6, doi: 10.1109/IPACT.2017.8244906. 
# keywords: {Switches;Hysteresis motors;Motor drives;Integrated circuits;Torque;Inverters;Brushless motors;Brushless DC (BLDC) 
# motor;Hysteresis current controller;Anti-Windup controller (AWC);Three-phase inverter},


# ==========================================================

# --- Parametros do motor ---
R  = 2.9              # Ohm       - resistencia de armadura
L  = 0.0202           # H         - indutancia de magnetizacao
M  = 0.0              # H         - indutancia mutua (nao usada em bldc_step)
Ke = 1.1       # V/(rad/s) - constante eletrica (FCEM)
J  = 1.0e-3           # kg.m^2    - momento de inercia do rotor
B  = 5e-4             # N.m/(rad/s) - coeficiente de atrito viscoso
Tl = 0.0              # N.m       - torque de carga aplicado ao eixo
P  = 4               # -         - numero de pares de polos
Kt = 1.1       # N.m/A     - constante de torque

# --- Parametros do SVPWM (chaveamento real do inversor) ---
Fsw        = 10000    # Hz - frequencia de chaveamento
PwmSamples = 200      # passos finos de simulacao por periodo Ts
                      # (maior = mais fiel ao chaveamento real, porem mais lento)

# --- Parametros de tempo da simulacao ---
Ti = 0.0              # s - tempo inicial
Tf = 0.4              # s - tempo final
Dt = 1e-6             # s - passo de integracao explicito
                      # (0.0 = automatico: dt = 1/Fsw / PwmSamples;
                      # informe um valor > 0 para sobrescrever o
                      # calculo automatico)

# --- injecao da nova carga Tl ---
Ttl = 0.2           # s - tempo de injecao da nova carga
Tlnew = 0.8         # N.m - Torque da nova carga

# --- Barramento CC ---
Vdc = 220              # V - tensao do barramento CC

# --- Controladores PI ---
KpOmega = 0.1142     # ganho proporcional - malha de velocidade
KiOmega = 28.7116     # ganho integral - malha de velocidade
KpId    = 126.92         # ganho proporcional - malha de corrente id
KiId    = 18221.237         # ganho integral - malha de corrente id
KpIq    = 126.92         # ganho proporcional - malha de corrente iq
KiIq    = 18221.237         # ganho integral - malha de corrente iq

# --- Referencia de velocidade ---
rpm = 900             # RPM (mecanico) - velocidade desejada

# --- Partida em malha aberta ---
# true/yes/on
# false/no/off
# Partida com Fonte de alimentação senoidal
MalhaAberta = 0

# --- Saida ---
file = closedloop_simulation.csv
